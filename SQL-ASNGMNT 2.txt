1>SELECT dname 
FROM dept 
WHERE deptno NOT IN (SELECT deptno FROM emp); 

2>SELECT ename,job 
FROM emp 
WHERE JOB='PRESIDENT';

3>SELECT ename,job 
FROM emp 
WHERE deptno=(SELECT deptno FROM dept WHERE loc='NEW YORK') AND comm>1000;

4>SELECT COUNT(*) as EMPS 
FROM emp 
WHERE deptno=(SELECT deptno FROM dept WHERE loc='CHICAGO');

5>SELECT empno,ename 
FROM emp 
WHERE deptno=(SELECT deptno FROM dept WHERE loc='CHICAGO');

6>SELECT ename,loc 
FROM emp e,dept d 
WHERE e.deptno=d.deptno ORDER BY d.loc;

7>SELECT COUNT(*) AS ECOUNT
FROM EMP
UNION
SELECT COUNT(*) AS DCOUNT 
FROM dept;

8>SELECT ename 
FROM EMP 
WHERE HIREDATE<(SELECT MIN(HIREDATE) FROM EMP WHERE JOB='MANAGER');

9>SELECT COUNT(*) as MGCOUNT
 FROM emp 
WHERE JOB='MANAGER';

10>SELECT ename,JOB 
FROM emp 
WHERE JOB NOT IN ('MANAGER','PRESIDENT');
11>SELECT ename,deptno
 FROM emp 
WHERE ename='SMITH';

12>SELECT ename,job 
FROM emp 
WHERE deptno NOT IN (SELECT deptno FROM DEPT WHERE loc='BOSTON');

13>SELECT ename,loc 
FROM emp e,dept d 
WHERE e.deptno=d.deptno;

14>SELECT ename 
FROM EMP 
WHERE sal<(SELECT sal FROM emp WHERE ENAME='MILLER') AND JOB='SALESMAN';

15>SELECT * 
FROM emp 
WHERE deptno=(SELECT deptno FROM dept WHERE loc='CHICAGO');

16>SELECT ename
 FROM emp 
WHERE sal>(SELECT MIN(SAL) FROM EMP WHERE JOB='MANAGER');

17>SELECT ENAME,JOB 
FROM EMP 
WHERE DEPTNO=(SELECT DEPTNO FROM dept WHERE DNAME='ACCOUNTING');

18>SELECT ENAME 
FROM EMP 
WHERE JOB='MANAGER';

19>SELECT *,
CASE
    WHEN JOB='SALESMAN' THEN 'SALES'
    ELSE 'NOT SALES'
END AS DETAILS
FROM emp;
 
20>SELECT *,
CASE
    WHEN SAL>1000 THEN sal
    ELSE 1000
END AS SALDETAILS
FROM emp;

21>SELECT *,
CASE
    WHEN SAL<1000 THEN 'LESS'
	WHEN SAL>1000 AND SAL<2500 THEN 'MEDIUM'
    ELSE 1000
END AS SALDETAILS
FROM emp;  